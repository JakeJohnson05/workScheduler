class shift {
	constructor(startTime, endTime, days, requirements) {
		this._startTime = startTime;
		this._endTime = endTime;
		this._days = days;
		this._requirements = (requirements) ? requirements:null;
	}
}